VẤN ĐỀ: API đặt hàng (production) không gửi email

Tóm tắt:
- Endpoint /api/order trên production trả về JSON 200 nhưng chỉ kèm message placeholder: "Đặt hàng thành công! (Chưa cấu hình RESEND_API_KEY)".
- Khi gửi request với dữ liệu không hợp lệ, function trả lỗi validation (ví dụ 400 address too short) — chứng tỏ function thực sự chạy.
- Khi gửi request hợp lệ (address đầy đủ), function trả 200 nhưng vẫn dùng placeholder => không gọi Resend.

Bằng chứng (curl):
- POST /api/order trả: {"success":true,"message":"Đặt hàng thành công! (Chưa cấu hình RESEND_API_KEY)"}
- GET / trả về index.html và static assets (/assets/...) được phục vụ ok.

Nguyên nhân khả dĩ:
1) Biến môi trường RESEND_API_KEY chưa được thêm trên Vercel, hoặc thêm nhưng không đúng tên.
2) Biến đã thêm nhưng scope không đúng (không ở Production) hoặc Vercel chưa redeploy để áp dụng biến.
3) Nếu file .env có key đã bị commit, key có thể bị lộ — cần rotate.

Việc đã thực hiện trong repo:
- Thêm `api/order.js` (JS handler) để đảm bảo Vercel deploy function.
- Cập nhật `vercel.json` để broaden build pattern (`api/**/*`) và thêm route explicit cho `/api/order` → `/api/order.js`.
- Đã test nhiều lần bằng curl từ local và production, xác nhận function chạy và trả placeholder khi key không có.

Hướng dẫn khắc phục (step-by-step):
1) Vercel → Project `mycay` → Settings → Environment Variables:
   - Tạo biến: Key = RESEND_API_KEY, Value = <your_resend_api_key>, Scope = Production (hoặc All Environments).
   - (Tùy chọn) Tạo RESEND_FROM = "Mỳ Cay Hà My <phucdat276@gmail.com>" nếu muốn override.
2) Sau khi lưu, redeploy project (Vercel thường tự redeploy; nếu không thì trigger manual redeploy).
3) Test lại bằng curl hoặc gửi đơn trên web:
   - Ví dụ test POST:
     curl -i -X POST https://mycay.vercel.app/api/order \
       -H "Content-Type: application/json" \
       -d '{"customerName":"Test","customerPhone":"0901234567","customerAddress":"Số 71C, Ngách 71, Ngõ 342 Khương Đình, Thanh Xuân, Hà Nội","cart":[{"menuItem":{"name":"Mì Cay","price":120000},"quantity":1}],"totalBill":120000}'
   - Mong đợi: 200 JSON success; và email đến inbox `phucdat276@gmail.com`.
4) Nếu nhận 500 sau deploy -> kiểm tra logs trên Vercel (Deployments → View Logs) để biết lỗi Resend (invalid key, rate limit, etc.).

An ninh / housekeeping:
- Nếu file `.env` chứa key đã bị commit, hãy rotate key (tạo key mới trên Resend, disable cũ) và xóa `.env` khỏi git history (BFG/git-filter-repo) hoặc ít nhất xóa commit và force-push.
- Thêm `.env` vào `.gitignore` để tránh lộ key tiếp.

Gợi ý nâng cao (tùy chọn):
- Thêm `GET /api/health` trả `{resend_key_present: true|false}` (safe) để nhanh kiểm tra production nhận env hay chưa.
- Cập nhật handler để trả thêm thông tin Resend response (ví dụ mail id) trong môi trường non-production hoặc chỉ log vào Vercel logs, không trả về client.

Người thực hiện tiếp: thêm biến `RESEND_API_KEY` trên Vercel và redeploy — sau đó test mail gửi thực tế.

-- Generated by repo assistant
